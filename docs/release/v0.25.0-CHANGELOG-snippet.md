## [0.25.0] - 2026-05-19

### Added
- WGC capture backend. (#537)
- NativeMenuInjector characterization tests. (#538)
- Added `scripts/diag/health-summary.ps1`.
- Added migration guide: `docs/release/v0.25.0-MIGRATION-GUIDE.md`.
- Added scaling rule + reliability empirics governance.

### Changed
- MainThreadDispatcher: `PumpIsAlive` now uses `volatile` and 7 bound-timeout sites in `GameBridgeServer`. (#535)
- NativeMenuInjector `InjectButton` reduced from 302 to 63 lines. (#222)
- `AssetBundleCache.Unload` default changed from `true` to `false`. (#534)
- `AssetSwapSystem` reflection now resolves a specific method overload. (#101)
- `PackDependencyResolver` and registry now use `StringComparer.Ordinal`. (#540, #541)

### Fixed
- Production hang fixed. (#535)
- Chicken sprites fixed. (#534)
- Star Wars 0/36 fixed. (#101)
- Block-git-stash hook completeness fixed. (#539)
- MSBuild `DeployToGame` silent no-op fixed. (#530)

### Removed
- None.

### Security
- None.
