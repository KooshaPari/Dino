# v0.25.0 Validation Checklist

Date: 2026-05-19

## BUILD
- [ ] `dotnet build src/DINOForge.sln -c Debug` succeeds with 0 errors across Debug configuration.
- [ ] `dotnet build src/DINOForge.sln -c Release` succeeds with 0 errors across Release configuration.
- [ ] `dotnet build src/Runtime -p:DeployToGame=true` succeeds with 0 errors.
- [ ] MSBuild warning `DF0530` is not emitted when target framework is explicit (verify with: `dotnet build src/DINOForge.sln -p:TargetFramework=net11.0 2>&1 | Select-String "DF0530"` returns no matches).

## TESTS
- [ ] Main suite passes with `3636/3641` passing (`dotnet test src/DINOForge.sln --verbosity normal` and confirm summary).
- [ ] Analyzer tests pass `119/119` (`dotnet test src/DINOForge.sln --filter FullyQualifiedName~Analyzer --verbosity normal` and confirm full pass count).
- [ ] Tier 3 property checks pass `152/152` covering `14200` cases (`dotnet test src/Tests --filter FullyQualifiedName~"Tier3" --verbosity normal` and confirm metrics).

## DETECTORS
- [ ] Wave-2 pattern #96 `logerror` is green (run detector suite and confirm 0 blocking findings for #96).
- [ ] Wave-2 pattern #232 `unbounded log` is green (run detector suite and confirm 0 blocking findings for #232).
- [ ] Wave-2 pattern #234 `test ids` is green (run detector suite and confirm 0 blocking findings for #234).
- [ ] Wave-2 pattern #235 `graphicraycaster` is green (run detector suite and confirm 0 blocking findings for #235).
- [ ] Existing CI detector gates are all green (run `git branch --show-current`; then execute the CI detector workflow/jobs in the same state as release branch).

## RUNTIME
- [ ] Deployed DLL hash matches build artifact (method: compare `Get-FileHash` output between `src/Runtime/bin/.../DINOForge.Runtime.dll` and deployed runtime DLL in game folder).
- [ ] Hang-fix log markers are present (method: launch game, reproduce startup path, and confirm markers in log file with `Select-String "HangFix"` / relevant marker text).
- [ ] Game launches and stays `Responding` for >= 120s (method: observe launcher/game process state and stability window).
- [ ] `F9` prints stat overlay (method: press `F9` in running game and confirm overlay appears).
- [ ] `F10` toggles mod menu (method: press `F10` in running game and confirm visibility toggles).

## DOCS
- [ ] `docs/release/v0.25.0-RELEASE_NOTES.md` exists (`Test-Path docs/release/v0.25.0-RELEASE_NOTES.md`).
- [ ] `docs/release/v0.25.0-MIGRATION-GUIDE.md` exists (`Test-Path docs/release/v0.25.0-MIGRATION-GUIDE.md`).
- [ ] `CHANGELOG.md` includes `[0.25.0]` entry (`Select-String "\[0.25.0\]" CHANGELOG.md` returns a match).
- [ ] `VERSION` file shows exactly `0.25.0` (`Get-Content VERSION | ForEach-Object { $_.Trim() }` equals `0.25.0`).

## NuGet
- [ ] `DINOForge.SDK` package built and packed for v0.25.0 (`dotnet pack src/SDK/DINOForge.SDK/DINOForge.SDK.csproj -c Release -p:Version=0.25.0` and verify `.nupkg` exists).
- [ ] `DINOForge.Bridge.Client` package built and packed for v0.25.0 (`dotnet pack src/Runtime/DINOForge.Bridge.Client/DINOForge.Bridge.Client.csproj -c Release -p:Version=0.25.0` and verify `.nupkg` exists).
- [ ] `DINOForge.Bridge.Protocol` package built and packed for v0.25.0 (`dotnet pack src/Runtime/DINOForge.Bridge.Protocol/DINOForge.Bridge.Protocol.csproj -c Release -p:Version=0.25.0` and verify `.nupkg` exists).
- [ ] NuGet metadata version is `0.25.0` for all three packages (`Get-ChildItem -Recurse -Filter "*.nupkg" | ? { $_.Name -match "DINOForge\.(SDK|Bridge\.Client|Bridge\.Protocol)" } | % { dotnet nuget verify $_.FullName }` equivalent check).

## GIT
- [ ] Branch is `fix/handle-connect-iter142` (`git branch --show-current` returns `fix/handle-connect-iter142`).
- [ ] All release commits are present on the branch (`git log --oneline fix/handle-connect-iter142 --not main --decorate` reviewed) and `origin/main` is rebased to include release commits OR PR exists (`git status` + PR link attached).
- [ ] Release tag `v0.25.0` is created and pushed (`git tag --list v0.25.0` and `git push --tags origin v0.25.0`).