# Pattern #113 Blocking Poll Sleep Allowlist
#
# Format: <path>:<line>  # <reason>
#
# This allowlist documents Thread.Sleep() calls within loops that have been
# reviewed and approved as safe. Entries are added when:
# - The sleep is in dead code or disabled blocks
# - The sleep is a one-shot synchronization point (not a polling loop)
# - Proper guards exist but static analysis doesn't detect them
# - The loop has alternative synchronization mechanisms
#
# To suppress a violation, add an entry below or mark the code with:
# // pattern-113-ok: <reason>

# Iter-79 D1 marked safe (dead code or one-shot sync):
src/Runtime/Plugin.cs:776  # dead code in disabled block (F10 key release)
src/Tools/McpServer/Tools/GameInputTool.cs:456  # one-shot focus settle for UI automation
src/Tools/McpServer/Tools/GameInputTool.cs:466  # one-shot focus settle for UI automation
src/Tools/McpServer/Tools/GameInputHelper.cs:232  # one-shot focus settle for UI automation
src/Tools/McpServer/Tools/GameInputHelper.cs:242  # one-shot focus settle for UI automation

# Iter-80 D2 analysis:
src/Runtime/Plugin.cs:646  # HMR signal file watcher polling — guarded by File.Exists
src/Runtime/Plugin.cs:755  # key event debounce sleep — one-shot wait within condition
src/Runtime/Bridge/GameBridgeServer.cs:786  # awaiting MCP pipe connection setup — guarded timeout
src/Runtime/Bridge/GameBridgeServer.cs:369  # guarded by thread-local state variable

# Iter-145 P0 (audit a9ec9a7a41fa48804): bring HIGH below threshold of 8
# Detector now uses repo-relative paths (relative_to(REPO_ROOT)) — entries MUST start with src/
src/Runtime/Bridge/KeyInputSystem.cs:162  # one-shot debounce guarded by ThreadInterruptedException -> break (cooperative cancellation)
src/Runtime/Bridge/KeyInputSystem.cs:166  # try/catch debounce settle — not a polling loop (iter-145/146)
src/Runtime/Plugin.cs:1679  # F9 debounce one-shot (iter-145)
src/Runtime/Plugin.cs:1700  # menu toggle debounce one-shot (iter-145)
src/Runtime/Bridge/GameBridgeServer.cs:925  # pipe accept backoff — bounded wait, not spin-poll (iter-145/146)
src/Tests/BridgeClientAsyncTests.cs:273  # intentional Infinite wait in cancel test (pattern-113-ok: test harness)
src/Tests/Integration/Tests/GameSandboxIntegrationTests.cs:112  # game sandbox settle — integration only
src/Tests/Integration/Tests/ParallelGameE2ETests.cs:129  # Unity boot wait — integration only
src/Tests/Integration/Tests/ParallelGameE2ETests.cs:151  # parallel E2E settle — integration only
src/Tests/UiAutomation/CompanionFixture.cs:95  # WinUI companion fixture settle
