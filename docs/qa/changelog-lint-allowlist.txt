# changelog-lint allowlist (Pattern #92 defense, task #251)
#
# This file lists EXTRA `### Section` headings that are permitted inside
# CHANGELOG.md version blocks beyond the canonical Keep-a-Changelog set
# (Added / Changed / Deprecated / Removed / Fixed / Security).
#
# Use this only for legitimate, project-specific structural sections that
# are NOT going to change shape — e.g. a permanent governance subsection
# that the project commits to maintaining across releases.
#
# Format: one heading label per line. Match is exact-string (whole heading
# after `### `) OR first-word match. Lines starting with `#` are comments
# and blanks are ignored.
#
# DO NOT add casual decay here. Every entry is a permanent waiver — the
# CI gate will not flag the entry again. Prefer fixing the changelog to
# widening this list.
#
# Companion script: scripts/ci/changelog_lint.py (--self-test for spec).
# Cross-reference: tasks #92/#250/#251.
#
# ----------------------------------------------------------------------------
# (intentionally empty — no project-specific section types are permanently
# whitelisted at this time. Add carefully.)
# ----------------------------------------------------------------------------
