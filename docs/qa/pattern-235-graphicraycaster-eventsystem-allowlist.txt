# Pattern #235: GraphicRaycaster without EventSystem Guard
# Allowlist for whitelisted GraphicRaycaster instantiations that don't require EventSystem guard.
# One file path per line. Lines starting with # are comments.

# DFCanvas calls Plugin.EnsureEventSystemAlive() immediately before AddComponent (iter-145 H1)
src/Runtime/UI/DFCanvas.cs
