name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    name: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      checks: write
      pull-requests: write

    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v4

      - name: Setup .NET
        uses: actions/setup-dotnet@4d6c8fcf3c8f7a60068d26b594648e99df24cee3 # v4.0.0
        with:
          dotnet-version: |
            8.0.x
          global-json-file: global.json
          cache: true
          cache-dependency-path: '**/packages.lock.json'

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj', '**/packages.lock.json') }}
          restore-keys: ${{ runner.os }}-nuget-

      - name: Restore dependencies (locked mode)
        run: dotnet restore src/DINOForge.CI.NoRuntime.sln /p:RestoreLockedMode=true
        env:
          CI: 'true'

      - name: Build dependencies (ordered)
        run: |
          for p in \
            src/SDK/DINOForge.SDK.csproj \
            src/Bridge/Protocol/DINOForge.Bridge.Protocol.csproj \
            src/Bridge/Client/DINOForge.Bridge.Client.csproj \
            src/Domains/Warfare/DINOForge.Domains.Warfare.csproj \
            src/Domains/Economy/DINOForge.Domains.Economy.csproj \
            src/Domains/Scenario/DINOForge.Domains.Scenario.csproj \
            src/Domains/UI/DINOForge.Domains.UI.csproj \
            src/Tools/Installer/InstallerLib/DINOForge.Tools.Installer.csproj; do
            dotnet build "$p" -c Release --no-restore --verbosity minimal || exit 1
          done
        # Note: Runtime excluded - requires Unity which is only available locally

      - name: Build
        run: dotnet build src/DINOForge.CI.NoRuntime.sln --no-restore --configuration Release /p:BuildInParallel=false
        # DINOForge.Tests.CliTools excluded from CI.NoRuntime.sln: it refs Tools.Cli (win-x64 RID);
        # Linux Release builds cannot resolve Cli ref metadata (CS0006 Debug/win-x64 vs Release).

      # Unit + Integration tests — build via dotnet test (not in CI.NoRuntime.sln; pulls Runtime)
      - name: Test (Unit)
        run: >-
          dotnet test src/Tests/DINOForge.Tests.csproj
          --configuration Release
          --verbosity normal
          --logger "trx;LogFileName=unit-test-results.trx"
          --collect:"XPlat Code Coverage"
          --results-directory coverage-results
          /p:GameInstalled=false
        env:
          CI: 'true'

      - name: Test (Integration)
        run: >-
          dotnet test src/Tests/Integration/DINOForge.Tests.Integration.csproj
          --configuration Release
          --verbosity normal
          --logger "trx;LogFileName=integration-test-results.trx"
          --collect:"XPlat Code Coverage"
          --results-directory coverage-results
          /p:GameInstalled=false
        env:
          CI: 'true'

      # ── Coverage gate ────────────────────────────────────────────────────────
      # Fails the build if line coverage drops more than 5 percentage points below the
      # stored baseline in docs/test-results/coverage-baseline.json.
      # On the first run (no baseline yet) it writes the baseline and succeeds.
      - name: Coverage gate (5% drop limit)
        if: always()
        run: |
          python3 - <<'PYEOF'
          import json, glob, os, sys, xml.etree.ElementTree as ET, datetime

          # ── Parse Cobertura XML produced by XPlat Code Coverage ──
          coverage_files = glob.glob("coverage-results/**/coverage.cobertura.xml", recursive=True)
          if not coverage_files:
              print("No coverage files found — skipping coverage gate (coverlet not installed).")
              sys.exit(0)

          total_lines = total_covered = 0
          for cov_file in coverage_files:
              tree = ET.parse(cov_file)
              root = tree.getroot()
              for pkg in root.findall(".//package"):
                  for cls in pkg.findall(".//class"):
                      for line in cls.findall(".//line"):
                          total_lines += 1
                          if int(line.get("hits", "0")) > 0:
                              total_covered += 1

          if total_lines == 0:
              print("Zero lines instrumented — skipping coverage gate.")
              sys.exit(0)

          current_pct = round(total_covered / total_lines * 100, 2)
          print(f"Current coverage: {current_pct}% ({total_covered}/{total_lines} lines)")

          baseline_path = "docs/test-results/coverage-baseline.json"
          os.makedirs("docs/test-results", exist_ok=True)

          if not os.path.exists(baseline_path):
              # First run — write baseline and succeed.
              baseline = {"coverage_pct": current_pct, "updated_at": datetime.datetime.utcnow().isoformat()}
              with open(baseline_path, "w") as f:
                  json.dump(baseline, f, indent=2)
              print(f"Coverage baseline written: {current_pct}%")
              sys.exit(0)

          with open(baseline_path) as f:
              baseline = json.load(f)

          baseline_pct = float(baseline.get("coverage_pct", 0))
          drop_threshold = 5.0  # threshold-ok: 5% acceptable regression window

          drop = baseline_pct - current_pct
          if drop > drop_threshold:
              print(f"COVERAGE DROP: {current_pct}% is {drop:.1f}pp below baseline {baseline_pct}%")
              print(f"Threshold: {drop_threshold}pp. Fix coverage regression or update baseline.")
              sys.exit(1)

          print(f"Coverage OK: {current_pct}% (baseline {baseline_pct}%, drop {drop:.1f}pp)")

          # Update baseline if coverage improved.
          if current_pct > baseline_pct:
              baseline["coverage_pct"] = current_pct
              baseline["updated_at"] = datetime.datetime.utcnow().isoformat()
              with open(baseline_path, "w") as f:
                  json.dump(baseline, f, indent=2)
              print(f"Baseline updated to {current_pct}%")
          PYEOF

      - name: SPEC-007 regression gate (validate-only, no game)
        shell: pwsh
        run: |
          pwsh -ExecutionPolicy Bypass -File scripts/game/prove-features-gate.ps1 -ValidateOnly

      - name: Setup Python
        uses: actions/setup-python@c8813ba1bc76ebf779b911ad8ffccbf2e449cb48 # v-latest
        with:
          python-version: '3.10'
          cache: 'pip'
          cache-dependency-path: 'src/Tools/DinoforgeMcp/pyproject.toml'

      - name: Test (Python MCP Server)
        run: |
          cd src/Tools/DinoforgeMcp
          python -m pip install --upgrade "pip==25.0.1"
          pip install -e ".[dev]" -q
          pytest tests/ -v --tb=short --junit-xml=python-test-results.xml

      - name: Export test results to docs
        if: always()
        run: |
          python3 - <<'PYEOF'
          import json, glob, xml.etree.ElementTree as ET, os, datetime

          results = {"timestamp": datetime.datetime.utcnow().isoformat(), "suites": []}
          for trx in glob.glob("**/*.trx", recursive=True):
              tree = ET.parse(trx)
              root = tree.getroot()
              ns = {'t': 'http://microsoft.com/schemas/VisualStudio/TeamTest/2010'}
              counters = root.find('.//t:ResultSummary/t:Counters', ns)
              if counters is not None:
                  results["suites"].append({
                      "file": trx,
                      "total": int(counters.get("total", 0)),
                      "passed": int(counters.get("passed", 0)),
                      "failed": int(counters.get("failed", 0)),
                      "skipped": int(counters.get("notExecuted", 0)),
                  })

          total = sum(s["total"] for s in results["suites"])
          passed = sum(s["passed"] for s in results["suites"])
          results["summary"] = {"total": total, "passed": passed, "failed": total - passed, "pass_rate": round(passed/total*100, 1) if total else 0}

          os.makedirs("docs/test-results", exist_ok=True)
          with open("docs/test-results/latest.json", "w") as f:
              json.dump(results, f, indent=2)
          print(f"Test results: {passed}/{total} passed ({results['summary']['pass_rate']}%)")
          PYEOF

      - name: Commit test results to docs
        if: always() && github.ref == 'refs/heads/main'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/test-results/latest.json || true
          git diff --staged --quiet || git commit -m "ci: update test results [skip ci]"
          git push || true

      - name: Publish Test Results
        uses: EnricoMi/publish-unit-test-result-action@30eadd5010312f995f0d3b3cff7fe2984f69409e # v2.16.1
        if: always()
        with:
          files: |
            **/*.trx
            **/python-test-results.xml
          check_name: test
          compare_to_earlier_commit: false

  build-runtime-windows:
    name: Build Runtime (Windows)
    runs-on: windows-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v4

      - name: Setup .NET
        uses: actions/setup-dotnet@4d6c8fcf3c8f7a60068d26b594648e99df24cee3 # v4.0.0
        with:
          dotnet-version: |
            11.0.x
          global-json-file: global.json
          cache: true
          cache-dependency-path: '**/packages.lock.json'
          include-prerelease: true

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj', '**/packages.lock.json') }}
          restore-keys: ${{ runner.os }}-nuget-

      - name: Restore dependencies (locked mode)
        run: dotnet restore src/Runtime/DINOForge.Runtime.csproj /p:RestoreLockedMode=true

      - name: Build Runtime (netstandard2.0)
        run: dotnet build src/Runtime/DINOForge.Runtime.csproj -c Release -p:TargetFramework=netstandard2.0 --no-restore --verbosity minimal

  sonarcloud:
    name: SonarCloud Analysis
    needs: build-and-test
    # PR quality gate is enforced by the SonarCloud GitHub App (KooshaPari_Dino).
    if: github.event_name != 'pull_request'
    continue-on-error: true
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v4
        with:
          fetch-depth: 0  # SonarCloud needs full history for blame analysis

      - uses: SonarSource/sonarcloud-github-action@e44258b109568baa0df60ed515909fc6c72cba92 # v3.1.0
        with:
          projectBaseDir: .
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
