name: Release Drafter
permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true


on:
  push:
    branches: [main]
  release-drafter:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Placeholder for custom release drafting logic
        run: echo "TODO: implement custom release drafting logic (phenoShared reusable workflow removed)"
