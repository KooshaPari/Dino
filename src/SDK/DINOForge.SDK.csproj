<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <AssemblyName>DINOForge.SDK</AssemblyName>
    <RootNamespace>DINOForge.SDK</RootNamespace>
    <Description>DINOForge SDK - public mod API, registries, schemas, pack loaders</Description>
  </PropertyGroup>

  <!-- Wrapped dependencies (ADR-008: wrap, don't handroll) -->
  <ItemGroup>
    <PackageReference Include="YamlDotNet" Version="16.*" />
    <PackageReference Include="Newtonsoft.Json" Version="13.*" />
  </ItemGroup>

</Project>
